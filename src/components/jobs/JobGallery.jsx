import React, { Component } from 'react';
import ImagesList from '../../ImagesList';

export default class JobGallery extends Component {
    render() {
        return(
            <ImagesList/>
        );
    }
}

